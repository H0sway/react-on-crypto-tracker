import React, { Component } from 'react';
import axios from 'axios';
import TrackerWrapper from './TrackerWrappper';

export default class TrackerWrapper extends React {
  render() {
    return (
      <div className="TrackerWrapper">
      </div>
    );
  }
}
